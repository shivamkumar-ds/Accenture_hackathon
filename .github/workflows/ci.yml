# Minimal CI (RC-2 remediation, item 5).
#
# Deliberately small, per explicit instruction: backend pytest, frontend
# lint + typecheck + build. Nothing more -- no deployment automation, no
# Docker publishing, no complex DevOps pipeline. Two independent jobs
# (backend/frontend never share a runner or depend on each other, matching
# how the two halves of this repo are actually developed and tested).
#
# Lint step added Phase 1.5 finding #10 -- ESLint didn't exist as a CI gate
# until now, so the `// eslint-disable-next-line react-hooks/exhaustive-deps`
# comments already in the codebase were no-ops (nothing was linting the
# project to suppress).
#
# No database service container: the backend test suite (see
# backend/tests/) builds its own in-memory SQLite database per test file
# and never connects to the real Postgres configured in DATABASE_URL, so
# nothing here needs to stand one up.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pytest
        run: python -m pytest -q

  frontend:
    name: Frontend lint + typecheck + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc -b

      - name: Build
        run: npx vite build
